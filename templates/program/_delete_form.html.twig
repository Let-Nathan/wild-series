<form method="post" action="{{ path('app_program_delete', {'id': program.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ program.id) }}">
    <button class=" btn btn-primary text-white d-flex align-items-center justify-content-center mt-2">Delete</button>
</form>